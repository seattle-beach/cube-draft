export class DummyUntapClient {
    getPack() {return new Promise(() => {});}
    createDrafter() {return new Promise(() => {});}
    pickCard() {return new Promise(() => {});}
    pickedCards() {return new Promise(() => {});}
}
